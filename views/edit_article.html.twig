{% extends 'layout.html.twig' %}

{% block content %}

    {% for article in userArticles %}
        <div>
            <h1> {{ article['title'] }} </h1>

            {% set img = article['image'] %}
            {{ img }}
            <img src="{{img}}"/>
        </div>
    {% endfor %}
{% endblock %}
